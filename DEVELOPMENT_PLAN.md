# My Office 开发与部署计划

- 编写日期：2026-09-18
- 工作时区：Asia/Shanghai (UTC+8)
- 文档状态：已按用户要求调整为先部署试用，再按反馈逐项迭代
- 当前进度：v0.2.0 已本机运行，像素界面和仅真实数据视图已完成；当前自动化测试覆盖真实观测、隐私、多项目、会话回收、同时间戳事件和可移植 hooks 生成。用户级 hooks 已分步授权配置，另一个项目的真实 Copilot CLI／SDK 会话已在接收端和页面验证；未连接业务系统。

> 2026-09-18 调整：用户要求先参考仓库做出可测试工具，不再等待 Q01-Q04 逐项文档验收。保留原安全和数据范围，先实现必要子集；本机试用授权不等于公司正式准入。入口与实际能力见 [README.md](README.md)。

> 2026-09-20 界面调整：用户选择 Munder Difflin 风格的 A+B 方案，即像素面板换肤和原创办公室场景重做。仍使用原生 HTML/CSS/Canvas 与现有 API/SSE；不迁移其 Electron/React 技术栈，不增加终端、记忆或 Agent 编排能力。角色动画是状态示意，不能扩大 hooks 所能证明的事实；具体界面与限制见 [README.md](README.md#像素办公室界面ab)。

> 同日布局细化：按用户提供的截图参考办公室平面布局、底部人物名册和右侧人物档案，并获准支持自定义昵称与职责。介绍仅保存在当前浏览器的同源本机存储中，与真实任务、请求摘录、状态和身份分离；不修改后端或 hooks。原始身份始终可查，自定义职责不是向 Agent 派发的角色指令。

> 同日真实观测收尾：按用户要求移除页面数据来源切换、模拟演示及手动上报。所有任务视图、统计和时间线只显示 `observed`，排除 `bridge_test`；无真实事件时显示空态。保留已有本地数据与兼容 API，不重启现有服务。42 项自动化测试、隔离浏览器人物介绍/跨标签/筛选检查及实际页面四种宽度检查通过；完整结果见 README。以下后续章节中的模拟/手动试用安排为历史阶段设计，不代表当前页面入口。

> 同日多项目接入分步授权：用户选择当前 Windows 用户的全部本机项目与仅元数据模式，随后仅批准先修改项目代码并测试，不批准本步骤写全局 hooks 或重启现有服务。v2 接收器区分 VS Code / Copilot CLI / 项目 / 会话，正文摘录已停止；另一路完成的本机分类推荐和手动分类覆盖保留并做兼容整合。v1 运行实例保持单工作区、旧字段协议，后续全局配置和重启需另行确认。此条覆盖旧的单项目范围和 120 字摘录安排；不扩大到远程环境或其他厂商 Agent。

> 22:08 后用户委托执行 VS Code 设置步骤：先备份用户设置，再仅在已有 `chat.hookFilesLocations` 中加入本项目 VS Code hooks 文件的绝对路径。已验证原六个 hook 路径及其他全部设置不变，JSON 和编辑器检查通过；本机接收端仍为 `all-local` / `metadata-only`。本步骤没有重启服务或配置 CLI；其他项目真实事件与工作区覆盖情况待后续验收。

> 23:01 跨项目无事件排查：接收端持续收到本项目事件，但没有另一个项目的观测。核对本机 VS Code 1.138.0 的实际路径校验器及两个窗口的 renderer 警告后，确认上一轮绝对路径条目被跳过；JSON 有效不代表 hooks 被加载。再次备份设置，仅将 My Office 条目改为受支持的 `~/.copilot/my-office/vscode-hooks.json`，新增专用用户文件并保持原八个观察事件与元数据边界。未修改其他项目、未配置 CLI、未重启服务；跨项目真实验收仍需一次修复后的新请求。

> 23:19 SDK 跨项目验收：修正路径后继续核实会话元数据，确认另一个项目的测试会话实际提供方是 `copilotcli:`，并非传统 VS Code Chat。用户明确批准继续配置 CLI／SDK 后，已备份并在用户 `.copilot\settings.json` 加入十一项内联观察 hooks；原设置与权限不变。向用户已有的专用测试会话发送同样的“只回复 OK”请求后，接收端与页面均出现该项目的真实 CLI／SDK 记录和会话结束事件，测试未改动业务文件。随后备份并移除本项目旧 My Office hook，避免新会话重复上报。八项针对性回归测试通过，无服务重启；传统 Chat、独立终端 CLI、远程环境和其他配置档尚未分别实测。

> 23:51 原业务会话缺失事件：用户继续在原业务聊天发问，23:42 的会话活动未到达接收端。对比 Agent Host 的纯生命周期日志，原会话最后一次配置刷新在 21:14，成功的专用测试会话在 23:19 刷新过配置；用户内联 hooks 于 23:17 才加入。已确认测试会话验收不能推广到仍使用旧配置的内存会话。下一步需要用户先保存文件并确认无其他运行中的本地 Agent，再重启本地 Agent Host、从原聊天验证；当前未执行重启，未读业务聊天正文，未修改其文件或权限，也未为此添加模拟动画。

## 1. 目标与已确认边界

制作适合 Chi 工作电脑使用的独立工具，回答四个问题：哪个 Agent 在做什么、推进到了哪里、为什么停住、什么时候需要 Chi 处理。

产品定位是观察现有工作，不接管电脑或任务编排。当前为原创像素办公室与结构化工作详情的双视图，仅显示真实 Copilot hooks 观测，不提供模拟数据或手动任务入口。

### 范围内

- 显示实际执行实例、会话、任务、阶段进展、阻塞原因和最近动作。
- 显示等待输入、等待内容审核及等待外部写入批准，但不代替原系统作出批准。
- 提供事件时间线及受控的成果入口。
- 按 DM 工程、周报/月报、知识整理、代码与自动化分类；分类不是自动创建的 Agent。
- 当前 Windows 用户的本机 VS Code / Copilot CLI／SDK 通过已获准的独立用户级 hooks 接入；页面仅展示真实观测，合成测试数据只在隔离验证中使用。跨项目已实测 CLI／SDK，其他客户端与环境不能据此假定成功。

### 范围外

- 不自动启动、派遣、恢复或终止 Agent，不提供任意命令执行接口。
- 不写回 ADO、M365，不发送邮件或消息，不自动 promote Obsidian Capture。
- 不采集完整对话、邮件正文、代码正文、工具返回原文、凭据或屏幕内容。
- 不启用外部模型摘要、生图、遥测、公网隧道、局域网共享或远程审批。
- 不安装 Electron、Docker、WSL，不设置开机自启、系统服务或计划任务。
- 不擅自修改其他 Agent hooks、审批规则、组织策略或长期技能。2026-09-20 用户已逐步批准本机全项目、仅元数据的观察配置：VS Code 专用用户文件与设置引用、CLI／SDK 用户设置内联 hooks，以及接通后的本项目重复观察配置清理；不改其他项目业务文件，所有配置变更先备份。

自研、开源和无需管理员权限都不等于公司批准。公司政策未知时，不绕过控制，也不将公司数据转移到个人设备试验。

## 2. 事实与待验证项

| 项目 | 已核实事实 | 尚不能据此推断的结论 |
| --- | --- | --- |
| 本机系统 | Windows 11 企业版，64 位 | 第三方软件或自研服务已获批准 |
| 已有工具 | Git 2.55.0、Node.js 24.20.0、uv 0.12.7、Python 3.14 | 新依赖的准入与运行兼容性 |
| 端口 | 检查时 19000、3000、8000 未被占用 | 下次启动仍可用；服务将只监听本机 |
| 工作区 | 创建本计划前为空；现有 app 中的独立试用实现 | 所有计划功能或真实集成已完成 |
| Copilot 接入 | 当前使用 VS Code Copilot；CLI 与 IDE Chat 是不同来源 | 可完整、实时观察所有 Chat 会话与子 Agent |
| 公司政策 | 未取得此工具的明确政策结论 | 可以安装、启动服务或读取业务元数据 |

### 参考仓库的使用方式

| 仓库 | 借鉴内容 | 复用限制与决定 |
| --- | --- | --- |
| [Star Office UI](https://github.com/ringhyacinth/Star-Office-UI) | 状态驱动的场景、轻量接入 | 不部署整包；代码 MIT，但美术资产禁止商用，不复制其素材 |
| [Clawd on Desk](https://github.com/rullerzhou-afk/clawd-on-desk) | provider 适配、会话区分、失联检测 | AGPL-3.0；当前只参考设计，不移植实现 |
| [Claude Office](https://github.com/paulrobello/claude-office) | 事件记录、状态机、会话回放 | 借鉴模型，不引入 Claude 执行或 AI 摘要依赖 |
| [Pixel Agents](https://github.com/pablodelucca/pixel-agents) | 像素角色、会话与工作区对应 | 当前 README 以 Claude Code 为已实现接入，其他 provider 路线图不当作可用能力 |
| [Munder Difflin](https://github.com/chaitanyagiri/munder-difflin) | 复古游戏面板、办公室分区及任务状态呈现 | 原创 Canvas 与样式，不复制角色美术；不引入编排、自动安装、远程连接或遥测 |

这些是选型筛查结果，不是完整许可证或安全审计。若后续复用代码、字体、图标或图片，必须逐项核实许可、保留必要声明并固定来源版本。

## 3. 技术路线

使用已有 Node.js 24 运行本机服务，浏览器显示 GUI。试用版使用 Node.js 内置 HTTP、文件操作和测试能力，前端采用 HTML、CSS、Canvas 2D 和 JavaScript ES modules。新增固定版本 Lucide 1.44.0 用于本地图标，安装时禁用 lifecycle scripts，锁文件与许可声明保留；未引入 Python 或前端框架。公司依赖准入仍需确认。

```text
模拟事件 / 手动上报 / 后续获批的独立 collector
                         |
                  统一事件校验入口
                         |
                 状态计算与本地事件记录
                         |
              本机 API + SSE 增量更新
                         |
         像素办公室 / 任务列表 / 详情 / 时间线
```

- collector 只负责将允许采集的来源字段转换为统一事件，不负责界面、不负责派活。
- 核心状态逻辑可以脱离 GUI 测试；模拟、手动、真实来源共用同一校验入口。
- 像素角色和场景自行制作；文字详情使用普通 HTML，避免信息只存在于画布中。
- 图片、字体和图标随应用本地提供，不依赖外部 CDN。
- 初期用有大小上限的本地事件文件保存最小元数据，不建立第二套业务知识库。若确有查询或并发需求，再评估 SQLite。
- 运行数据、密钥、日志不进入源码仓库，也不自动进入 Obsidian 或任何云同步路径。

### 状态契约草案

以下是目标设计，不是全部已实现的接口。v0.2.0 在模拟/手动任务基础上增加单工作区 hook 接收、脱敏身份、有界去重、旧事件拒绝和五分钟新鲜度；没有历史补读、可靠队列、完整任务语义或独立心跳。真实投递尚待验收。

| 概念 | 规则 |
| --- | --- |
| 身份 | 区分 provider、agentId、sessionId、taskId、eventId；多个会话不得因为同名而合并 |
| 来源 | sourceMode 为 simulated、manual 或 observed；观测来源再注明 hook、API 或 experimental-log |
| 会话状态 | idle、running、waiting、stopped、unknown；进程存在不等于正在执行任务 |
| 任务状态 | queued、in_progress、waiting_input、waiting_review、blocked、completed、failed、cancelled |
| 活动类型 | 检索、编辑、执行、验证等作为 activity，不混入任务完成状态 |
| 新鲜度 | lastObservedAt 与 freshness 独立表示；失联标为 stale/unknown，不自动变为 idle 或 completed |
| 进展 | 采用具名里程碑及其状态；分母未知时不显示百分比，不把工具次数或运行时间当完成率 |
| 审批 | 只记录等待哪类人工决定及来源；内容审核通过不等于外部写入获批 |
| 时间 | 保存带时区的事件时间与接收时间，界面统一显示北京时间；保留两者差异 |
| 成果 | 仅登记允许展示的引用；没有成果或验证证据时，不宣称已验证或已发布 |

完成会话的一轮响应不等于完成业务任务。手动完成标识为用户声明；模拟完成始终保留模拟标签；真实任务完成必须来自定义好的证据事件。

## 4. 分阶段开发与验收

不预设交付日期。按最新要求先让用户试用必要子集，再围绕反馈逐个解决问题；技术检查通过不等于用户验收或公司准入。

| 阶段 | 核心问题 | 交付物 | 验收标准 | 当前状态 |
| --- | --- | --- | --- | --- |
| P0 范围与准入 | 能做什么，什么需要额外许可？ | 本计划、政策与数据权限检查项 | 范围明确；未知批准保持未知；涉及运行、下载、采集的动作在相应关口确认 | 用户已要求本机试用；公司准入未核实 |
| P1 状态核心 | 什么算工作、进展、等待和完成？ | 统一事件协议、状态计算、模拟事件序列、单元测试 | 身份不串线；状态与新鲜度分开；重复、乱序、失联与恢复有确定结果 | 模拟/手动与 hook 事件子集通过；完整任务语义未实现 |
| P2 交互界面 | 是否能快速看懂并找到需要处理的任务？ | 原创办公室、列表、详情、时间线、筛选、模拟与手动入口 | 来源标签始终可见；空、等待、错误、过期状态完整；角色与列表使用同一状态 | 首版已运行、浏览器检查通过，待用户试用 |
| P3 本机试点 | 能否可控地启动、运行、停止和移除？ | 加固的本机服务、启动停止说明、发布包、验收记录 | 安全检查通过；模拟数据端到端可用；无非预期外联；无残留进程或系统配置改动 | localhost 服务与停止流程已验证；非生产准入 |
| P4 单来源采集 | 能否可信地显示一个真实 Copilot 执行实例？ | 接口可行性结论、能力表、可关闭的独立 collector | 获批来源最小化读取；真实会话验证；不支持的事件明确未知；失败不干扰 Copilot | 单工作区配置已获批启用，合成测试通过；待真实请求验收 |
| P5 工作上下文 | 如何关联成果与业务而不扩大权限？ | 工作分类、受控成果入口、业务关联设计 | 先手动关联；外部读取另行确认；不带写权限、不复制业务正文、不改变审批流程 | 未开始 |

P3 通过后即得到可用的模拟/手动版本；P4 不是承诺一定成功的集成。如果没有稳定或获准的观测来源，保留手动模式并记录限制，不把推测包装成实时状态。

### 界面布局

- 顶部：连接状态、来源模式、最后更新时间、需要 Chi 处理的数量。
- 左侧：工作分类和状态筛选。
- 中央：像素办公室与任务列表切换；无真实任务时显示空状态，不生成假员工。
- 右侧：选中任务的阶段、最近动作、阻塞原因、来源与成果入口。
- 底部或独立视图：事件时间线，支持按任务筛选。
- 等待处理区：用于定位需要处理的事项，不提供 ADO 发布、邮件发送或权限批准按钮。

### 验证范围

- P1：使用 node:test 验证状态转换、来源隔离、重复事件、乱序事件、未知字段、时钟偏差、失联和重连。
- P2：使用浏览器自动化验证筛选、选中、手动上报、模拟开始/停止；检查桌面与窄屏截图、文字不重叠、场景非空、来源标签不被隐藏。测试工具的安装仍需准入。
- P3：验证监听地址、写入鉴权、Origin/Host 校验、请求大小限制、非法输入、目录越界拒绝、事件存储上限、浏览器重连和服务停止。
- P4：合成测试不替代真实会话验收；分别记录已观测、推断和不支持的能力。

## 5. 公司电脑部署方案

### 启动前关口

- [ ] 确认公司对自研本机工具、监听端口、源码下载与依赖安装的要求。
- [ ] 确认首轮只使用模拟/手动非敏感测试数据，不包含真实项目名、人员或业务内容。
- [ ] 如需依赖，通过公司允许的软件源取得，锁定版本并完成要求的扫描。
- [ ] 核查所有随包素材、图标与字体的许可和来源。
- [ ] 核查工作目录与运行数据目录的同步/备份属性，不默认认为磁盘目录不会同步。

### 运行方式与安全要求

- 单用户、手动启动，使用普通用户权限；不要求管理员提权。
- 仅监听 127.0.0.1；默认候选端口 19000，占用时明确报错或选择另一个本机端口，不终止其他进程。
- 不创建防火墙入站规则，不使用 0.0.0.0、公网代理或隧道。
- 对状态写入和 collector 接入实施鉴权，严格校验 Host、Origin 和请求体；本机监听本身不是完整安全边界。
- 密钥本地生成并受访问权限保护，不进入 URL、源码、日志、截图或模型上下文。
- 不提供任意文件读取、目录遍历、上传执行或 shell 接口；成果入口只允许登记的范围。
- 用户输入按文本渲染，禁止执行事件内容中的 HTML、脚本、指令或链接协议。
- 无模型服务调用，无遥测与外部运行时资源；通过启动配置、代码检查及可用的网络观测验证，不把一次检查当成长期保证。
- 建议事件元数据最多保留 7 天并设置文件容量上限，具体上限在 P3 验收前确定；原始日志不复制到工具存储中。
- 服务重启后，旧 observed 状态须等待新观测确认，不能继续显示为正在工作。

### 部署与退出步骤

1. 从已验收源码产生有版本标识的本地发布包，包含静态资源、运行入口及必要许可声明。
2. 执行只读预检，确认 Node 版本、端口、配置和运行目录权限；缺项时停止，不自动安装或提权。
3. 手动启动，显示实际的 localhost 访问地址；首次进入明确标为模拟/手动测试模式。
4. 完成 P3 验收清单后再决定是否作为日常工具使用。
5. 手动停止服务，确认监听端口释放；关闭浏览器不等于服务已停止。
6. 回退时先停服务再切换上一个已验收版本；存储格式不兼容时拒绝静默覆盖。
7. 移除时明确列出应用和运行数据；删除用户数据需确认，不触碰 VS Code、Agent 或 Obsidian 原始文件。

## 6. 真实采集的单独关口

已核实 VS Code 1.138.0 / 内置 Copilot 0.66.0；用户确认存在 `/hooks`，并于 2026-09-20 明确批准当前工作区独立观察 hooks 与服务重启。未授权读取历史会话、配置其他工作区或更改既有 hooks。当前实现使用官方 Preview hooks，不读取内部日志。

1. 优先查验受支持的事件/API；文档能力与本机版本能力分开记录。
2. 如无适用接口，再评估本地 session/debug 数据，但只读用户明确选择的测试会话，不递归扫描所有工作区。
3. 非稳定数据格式放入 experimental collector，支持版本检测、格式不识别时停止采集以及一键关闭。
4. 在来源侧只提取白名单字段；敏感原文不进入状态服务、错误日志或第三方模型。
5. 不注入扩展内部、不绕过授权、不读凭据、不自动修改 Copilot 配置。
6. 首次真实验证只选一个不含公司机密的任务；会话开始、工具动作、等待、停止、失联分别验证。
7. 子 Agent 支持另做能力检查。看不到的实例不得模拟出来，也不把一次工具调用当作独立 Agent。

业务系统接入继续遵守：ADO 变更先本地 Markdown 审批；内容批准与写回批准分开；Obsidian 的事实确认不等于 promote 批准。My Office 只显示这些状态，不改变规则。

## 7. 逐个攻克的问题队列

| 编号 | 问题 | 完成证据 | 状态 |
| --- | --- | --- | --- |
| Q01 | 一个任务怎样表达“在做什么、到哪一步、是否需要 Chi”？ | 最小事件协议与六组场景的预期状态核对 | 模拟/手动设计已用于试用；待用户反馈 |
| Q02 | 相同事件是否总能得到相同状态，异常会不会误报完成？ | 状态计算的自动化测试结果 | 现有子集通过；hook 去重、旧事件拒绝和过期已实现，无独立心跳 |
| Q03 | 办公室和列表怎样用同一份状态表达真实工作？ | 可操作原型及交互/布局验收 | 模拟/手动界面已可试用 |
| Q04 | 如何本机启动且控制访问、数据保留与退出？ | P3 部署与安全验收记录 | 已提供本机启动入口；待日常试用反馈 |
| Q05 | 当前 Copilot 究竟能提供哪些可信观测？ | 针对具体版本的接口与能力表 | 官方八类 Preview 事件已核实；本机投递与子 Agent 归属仍待验收 |
| Q06 | 如何以最小权限接通一个真实测试会话？ | 独立 collector 的真实会话验收记录 | 代码与活动配置就绪；下一步由用户发非敏感请求 |
| Q07 | 如何加入工作分类、成果与审批背景？ | 不扩大读写权限的关联验收 | 未开始 |

### 下一步：用户试用

先按 README 中“接通当前 Copilot 窗口”发一个非敏感只读请求，验证真实观测标签、工具事件与本轮结束。通过后再验证同工作区第二会话；其他工作区注册需另行确认，不因试用成功自动扩大到真实日志或外部系统。

### 保留的 Q01 验收场景

目标设计使用具名任务“整理测试资料”，包含检索、草稿、验证三个阶段；输入只使用虚构数据。以下六组场景保留为后续完整状态契约的验收依据；当前试用版未全部覆盖：

1. 排队 -> 执行 -> 具名阶段完成 -> 有证据的任务完成。
2. 执行 -> 等待 Chi 输入 -> 输入后继续；没有输入时不自动恢复。
3. 草稿形成 -> 等待内容审核；不因此显示为已批准写回或已发布。
4. 工具失败 -> 阻塞 -> 重试恢复；保留失败记录，单次工具失败不必等于整个任务失败。
5. 心跳丢失 -> 状态过期 -> 重连；过期不冒充空闲或完成。
6. 两个会话并行 -> 收到重复/乱序事件 -> 重建状态；身份与来源不串线，旧事件不覆盖已确认的新状态。

Q01 的最终验收是：仅看任务列表和详情，就能准确回答四个目标问题；未知内容明确未知。真实 Agent 接入仍须单独验证并确认采集范围。

## 8. 执行记录

| 日期 | 事项 | 结果 | 未完成项 |
| --- | --- | --- | --- |
| 2026-09-18 | 方向与本机条件评估 | 已确定独立本机观察工具方向；发现参考项目的素材、网络默认值与接入限制 | 公司准入、实际运行兼容性和 Copilot 观测能力尚未验证 |
| 2026-09-18 | 建立开发部署计划 | 明确阶段、关口、问题队列与 Q01 验收场景 | 仅完成计划编写，所有实现与运行验收未开始 |
| 2026-09-18 | 按用户要求先部署试用 | 独立 Node.js 工具已运行于本机；8 项测试与浏览器主要流程通过；原创像素素材、本地图标、模拟与手动任务 | 等待用户体验反馈；真实 Agent、完整事件协议、业务系统集成未开始；公司准入未核实 |
| 2026-09-20 | 单工作区 Copilot Bridge | 经用户确认新增独立 hooks 并重启为 v0.2.0；12 项测试及隔离浏览器合成验证通过；正式实例等待事件 | 真正会话/子 Agent 投递未验收；多工作区与业务集成未开始；未读取历史正文 |
| 2026-09-20 | 人物任务可读性 | 用户批准仅在本机内存显示每轮请求最多 120 字的摘录；新增目标、可读工具活动、介入状态说明与单人物记录；19 项测试及隔离浏览器验证通过，含大快照误断线修复；经批准重启，手动任务保留，正式实例连续 3 次状态流更新正常 | 历史请求不补读；真实请求摘录待下一轮输入验证；子 Agent 委派、权限/提问/审核与业务进度仍未知 |

后续每轮只更新已实际发生的结论、验证结果和阻塞项；不预填完成状态，不把模拟验收写成真实集成通过。
